# 00 Input

Người dùng đã tự cập nhật `AGENTS.md` và yêu cầu migration thư mục `.harness/` thành `.harness/`.

Ràng buộc:

- Repo này là kiến trúc Harness, không phải source code application cụ thể.
- Migration phải cập nhật các tham chiếu trong hướng dẫn, template, script, installer, README và quy trình verify.
- Tránh thay đổi ngoài phạm vi migration đường dẫn.
