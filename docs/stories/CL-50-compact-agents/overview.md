# CL-50 Overview

## Status

completed

Compact the canonical and installed shared `AGENTS.md` surface to the
command-first lifecycle while preserving the installer parity contract. The
shared block is generated from this file, so no duplicate installer literal is
introduced.
