# CL-70 Exec Plan

1. Capture healthy source/DB and incomplete H5 baselines.
2. Add fail-fast missing-CLI installer preflight and negative-path coverage.
3. Add repeatable state and distribution qualification modes.
4. Run the two modes as real normal-lane dogfood tasks with structured proof,
   detailed traces and capsules; exercise block/resume on the parent task.
5. Rebuild the packaged CLI, run the full validation ladder, record CL-70 proof,
   trace, capsule, story status and execution-plan amendment.
6. Audit every required environment/evidence row and confirm H5 from durable
   current-state evidence before completion.

Stop for human direction if qualification requires deleting/downgrading the
retained DB, expanding platform distribution, weakening a gate, or overwriting
unknown user/product files.
